# game_memory.md — Cross-Session Progress Log

Purpose: ultra-brief record of what's done, updated at the end of every work iteration,
so a fresh session can continue without re-reading the whole history.
Keep entries short. Newest entry at the top of the log. Full details live in
IMPLEMENTATION_PLAN.md and per-phase notes.

## Current status

- **Phase**: Phases 1-9 committed & pushed; seabed texture swap + Phase 9 perf fix committed (**6f4d02b**, **4de237b**). **Phase 10 — First Complete Host Roster BUILT + verified — UNCOMMITTED, approval gate** (Log 30). FLORA_DENSITY 2.0. Next: Phase 11 — Cthulhu Minions.
- **Log 30 (Phase 10 — Host Roster + abilities + crab crawl + sideways fixes, awaiting approval)** — a curated set of distinct playable hosts. Approved roster (via AskUserQuestion): clownfish (agile starter), reef crab (defensive), grouper (defensive bruiser), barracuda (skirmisher), reef shark (playable apex). Any other wild fish = generic host.
  - **Data model** (species.ts): SpeciesDef gains `archetype`, `locomotion` ('swim'|'crawl'), `attack: AttackDef` (name/damageMult/reachMult/cooldown/lungeSpeed/sweep), `ability: AbilityDef` (kind/name/cooldown/duration/desc), `connectionMult`, `identity`. `ROSTER` map keyed by creature id layers curated values onto `hostProfileFromCreature`; unlisted creatures get generic defaults. `MovementDef` now exported.
  - **Per-host ATTACK** wired into PlayerCombat (reads `fish.species.attack`): damage×mult, reach×mult, per-host cooldown/lungeSpeed; `sweep:true` (shark) widens the bite cone to 0.1 (devours a wide arc). Removed hardcoded LUNGE_SPEED/ATTACK_CD.
  - **Ability system** — NEW `PlayerAbility.ts`, tapped on **Q** (Input.consumeAbility, latched like G). slip=evasive dart+i-frames (clownfish), brace=halve damage 3s (crab), burst=1.9× speed surge + scatter prey (barracuda), inhale=wide devour-arc + heal (grouper), frenzy=1.5× speed + i-frames + 10% heal (shark). Support hooks added: `SwimController.boost(mult,dur)`, `PlayerCombat.guard()/iframes()/heal()`. Cooldown-gated; resets on possession (fresh host = ready). HUD `#ability-hud` chip (Q badge + name + cooldown fill; cyan when ready, purple when active); possess toast shows `— <Ability> [Q]`; identity hint on possess; controls-help shows Q. **Verified live: crab Brace → guarding=true; shark Frenzy → boostT=4.**
  - **Per-host CONNECTION cost**: `PlayerConnection.update/riseRate` take `connMult`; GameApp passes `fish.species.connectionMult` (crab 0.6 quiet → shark 1.4 loud). Passive-resonance pace mirrors it.
  - **SIDEWAYS FIXES** (user bug): (1) swimmers — root cause was body facing VELOCITY (lags via turnRate) while the camera follows the mouse instantly, so big low-turn hosts (shark) trailed the camera and drifted sideways. FIX in SwimController orientation: face the AIM (camera dir) at `orientRate=max(turnRate*1.8,4)` — turnRate is purely visual (handling is accel/drag), so this is safe. **Verified: shark 1° off at 61°/s steering, 2.5° at 151°/s (was ~sideways).** (2) CRAB — genuinely mis-baked: the wide claw-span axis fooled the longest-axis heuristic. FIX: `modelYaw: Math.PI/2` in creatures.ts → claws lead +Z (verified screenshot). Diagnosed ALL host models via forced `possession.takeOver` + posed renders: only the crab was mis-baked; grouper/barracuda/silverside/wrasse/shark were all statically correct.
  - **CRAB CRAWL locomotion** (user ask: "controls different, jump+attack, not sideways"): `SwimController.crawlUpdate()` branch when `locomotion==='crawl'` — grounded seabed walker, WASD moves horizontally camera-relative, **Space=JUMP** (CRAB_JUMP 9, CRAB_GRAVITY 22), gravity re-grounds it, faces walk direction, stays upright. Shared solids+bounds collision factored into `collideSolidsAndBounds()`. **Verified: rests exactly on seabed (1.41m=radius), walks 2.6 m/s, jumps 1.85m, stays grounded.**
  - tsc + `vite build` clean; game loads with no console/runtime errors (only pre-existing coral `side undefined` warnings). Files: species.ts, creatures.ts, PlayerCombat.ts, PlayerConnection.ts, PlayerAbility.ts (new), SwimController.ts, Input.ts, GameApp.ts, index.html, style.css. UNCOMMITTED (approval gate: user approves roster + host feel). NOT-yet-done polish: per-host camera tuning (uses generic profile), explicit per-host Dominance reqs (current eligibility already gates), balance pass.
- **Log 29 (Seabed PBR texture swap → coast_sand_rocks_02, KTX2 kept, uncommitted)** — replaced the Shallow Veil ground texture (was coral_fort_wall_02, diffuse+normal only) with the user's full PBR set `assets/textures/coast_sand_rocks_02_1k` (diffuse / normal_GL / ARM / displacement, Poly Haven CC0).
  - **Pipeline**: `TerrainMaps` (types.ts) extended with optional `armMap` + `displacementMap`. `GameApp.loadSeabedMaps()` loads all four; `ShallowVeil.build` clones + tiles (colour/normal/ARM repeat **110**, displacement repeat **22** = deliberately low-frequency so the 300-seg mesh resolves it as gentle relief instead of aliasing into vertex spikes). `Terrain.build` binds one ARM texture to `aoMap = roughnessMap = metalnessMap` (R/G/B), sets `roughness=metalness=1` so the map passes through, and `displacementMap` at **scale 0.35 / bias −0.175** (±17 cm, centred on zero). **Collision is the analytic `heightAt` — untouched — so terrain shape + collision are preserved**; displacement is render-only and subtle.
  - **KTX2**: new `scripts/encode-ktx2.mjs` (npm `ktx2-encoder` wasm Basis encoder + `jpeg-js`/`pngjs` decoders), UASTC + Zstd + baked mips, `uastcLDRQualityLevel 3`. Diffuse encoded sRGB-transfer/perceptual; normal/ARM/disp linear. `AssetLoader.loadKTX2()` added; `SEABED_ENCODING` const in GameApp switches `'ktx2'` (default) vs `'image'`. Added `**/*.ktx2` to vite `assetsInclude`.
  - **Comparison (measured on real GPU via browse)**: disk **7067 → 4911 KB (−30%)** (diffuse *grew* 820→1329 KB because the jpg was already small; normal/arm/disp shrank a lot). Seabed VRAM **~21 MB RGBA → 5.33 MB** (transcoded to **ASTC 4×4** + 11 mips = ~4× less). Quality **visually identical** in A/B screenshots (UASTC near-lossless). **KTX2 kept.** NOTE: both encodings still bundle (source-image import path is statically referenced for the A/B switch) — dropping the `'image'` path later stops shipping the unused ~6.4 MB of PNGs.
  - **Verified**: tsc + `vite build` clean; KTX2 path loads with no console/shader errors; `map.isCompressedTexture=true`; displacement subtle with no spiking; terrain contour intact. Uncommitted; dev server at :5173 for user playtest. Pre-existing harmless `THREE.Material 'side' undefined` (coral) warnings remain.
- **Log 28 (Phase 9 — Connection System, awaiting approval)** — the game's central pressure. NEW `src/player/PlayerConnection.ts` (character-level 0..1, survives host swaps).
  - **Continuous rise**: `value += RISE_PER_SEC(0.010) × sensitivity × dt`. `sensitivity = SENS_BASE(0.55) + hostLength × SENS_PER_LEN(0.22)` — bigger host = faster (verified 6 m shark rises **2.83×** a 0.5 m fish; "powerful hosts cost more signal"). ~100 s to full at sens 1.0, no swaps.
  - **Fresh-possession relief + contamination anti-exploit**: `possess(speciesId)` drops connection by `max(0.04, FRESH_REDUCTION(0.45) × freshness)`, then marks that species contaminated (=1, decays `0.04/s` → fresh in ~25 s). Verified: first grouper grab −0.45, immediate re-entry −0.04. Wired into `onPossessed` (both guaranteed + risk paths); `showHint` teaches "familiar body" when freshness<0.5.
  - **Full = run ends**: `onFull` → new shared `GameApp.endRun(title, sub, variant)` (refactored from onHostDeath) sets `combat.dead`, shows death-screen "THE SIGNAL TAKES HOLD" with `.signal` eldritch styling. Verified.
  - **Prominent HUD** (`#connection-hud` top-center, widest bar per GAME_DESIGN §19): label + % + bar; tier classes rising/high/critical escalate colour + throb. `#connection-warning` ("CONNECTION CRITICAL — POSSESS A FRESH BODY") at critical. `#connection-vignette` eldritch magenta, opacity from level, pulses at critical. Tiers: calm<0.4, rising≥0.4, high≥0.7, critical≥0.9.
  - **Escalating audio** (`Sfx.setConnectionDrone(level)` persistent low saw+sub drone fading in past **70%** [moved from 40% per user]; `Sfx.heartbeat(intensity)` lub-dub only from **70%**, interval 1.4→0.42 s as level climbs). Ancient-entity voice sound deferred (user providing later).
  - **Passive Resonance** [user request]: Resonance now also trickles up over time at EXACTLY the Connection rise rate — `PlayerConnection.riseRate(hostLength)` (new static) driven into `PlayerResonance.tickPassive(dt, rate)` from `updateConnection`. Verified samePace=true, ~152 s to fill passively at starter (eating still far faster); scales with host size like Connection. So the means to escape builds alongside the pressure.
  - **Debug**: F3 overlay shows `connection X% (frozen)`; ` (Backquote) toggles freeze; `__game.setConnection(v)` / `freezeConnection(b)`.
  VERIFIED (browse real GPU): rise scaling, fresh/contam drops, full→run-end, critical HUD screenshot (bar + warning + vignette); tsc + build clean; no console errors.
- **Log 27 (Phase 8 — Risk Possession, awaiting approval)** — an instant, can-fail alternative to the guaranteed hold-F channel.
  - **Input**: tap **G** (`Input.consumeRisk()`, latched on non-repeat keydown while locked). Requires a locked target + full Resonance; spends Resonance whether it lands or not.
  - **Chance** (`PlayerPossession.riskChance`): `RISK_BASE 0.12 + matchup*0.35 + weak*0.5 + rank*0.02`, clamped [0.1, 0.9]; instant 0.9 if already stun-ready. `matchup` = normalized `requiredFrac` (size+Dominance ease), `weak` = 1−health01. Both size AND health move odds. Verified: full grouper 13%, full clownfish 25%, weak grouper 53%. Shown live in the prompt, colour-tiered good/fair/poor (≥66/≥33/<33).
  - **Success** → shared `takeOver()` (refactored out of `complete()`): swaps host at target's size/growth/HP. **Failure** → `combat.takeDamage(maxHP × lerp(0.18,0.42, 1−chance))` (longer odds hurt more), `target.provoke()`, `ecosystem.alertPrey()` (new: bumps playerThreatT so prey flee), `camera.punch`, `sfx.possessFail()` (new dissonant buzz), red `#possess-flash.fail` + "Snatch failed" toast, `RISK_COOLDOWN 1.6s`. Verified: forced-fail leaves host unchanged, −38.9 HP, resonance→0, cd=1.6; forced-success swaps host.
  - **UI**: possess-prompt reworked to column layout — name + `pp-actions` (`HOLD F possess` shown only when eligible + `G snatch XX%` always when charged). Controls hint adds `G snatch`. Guaranteed hold-F path UNCHANGED (still reliable). Risk works on ineligible targets (e.g. full-health grouper) — the whole point.
  VERIFIED (browse real GPU): chance readouts, forced success/fail outcomes, prompt screenshot (GROUPER · G snatch 13% red, no HOLD-F line since ineligible); tsc + build clean; clean reload (the one "Failed to fetch" was a transient reload/build collision).
- **Log 26 (4 pre-Phase-8 polish fixes, awaiting approval)** —
  1. **HUD relayout** — the 3-bar bottom stack looked bad. Growth (stage+length+bar) MOVED to top-left under Dominance (new `#growth-hud`, top:74px). Bottom-center is now ONLY Host Health (single bar). Resonance shown "differently": a radial `#resonance-gauge` (conic-gradient donut + ◆ core + label) bottom-right; JS sets `--pct`, toggles `.full` (cyan-purple glow) / `.needed` (red pulse). Removed old `#resonance-bar/-fill/-label`. `beginPlay` unhides growth-hud + resonance-gauge.
  2. **Possess bars gated** — `PlayerPossession.markEligible()` now only sets `stunReady` (the purple DamageBars) while RIGHT-MOUSE is held AND within `REVEAL_RADIUS=42 m`. No more map-wide "all possessable hosts" reveal on rank-up; you see nearby takeable fish only while targeting. Verified: rmb-off → 0 stunReady; rmb-on → near=true, far=false.
  3. **Same-tier possession at start** — `Dominance.canFreelyPossess` `>` → `>=` (CLASS_PEER_RANK unchanged). A Drifter can now freely possess weak-tier fish (clownfish/fry/other prey) from the start; grouper still gated. Verified at Drifter: clownfish/fry free=true, grouper=false.
  4. **Shark rework** (`do this properly`) — creatures.ts shark maxSpeed 9→12.5, accel 18→26, turnRate 2.3→2.1, sense 46→52. Creature.ts: apex `wantsToHunt()` always true (always prowling); hunt boost apex 1.3→1.85 with NO close-range brake (was `playerDist<6 ? 0.6`); hunt desired vec apex ×3.2 (straight-line commit); apex uses light separation (plows through shoals); new `apexSweepEat()` in fishMove devours ANY edible creature it overlaps (wide maw `radius+2.2`), not just its target. Verified: charges ~20 m/s (was braking/slow), eats through clusters.
  VERIFIED (browse real GPU): all four — HUD layout screenshot, reveal gating, same-tier free-possess, shark charge speed + kills; tsc + build clean; no console errors.
- **Log 25 (Resonance meter + target-gated possession, awaiting approval)** — added a possession COST so body-swapping isn't free.
- **Log 25 (Resonance meter + target-gated possession, awaiting approval)** — added a possession COST so body-swapping isn't free.
  1. **Resonance meter** (`src/player/PlayerResonance.ts`) — a CHARACTER-level charge (survives host swaps, NOT tied to the fish), 0..1. Eating fills it (`onFeed` biomass now feeds BOTH `growth.feed` and `resonance.feed`; `RESONANCE_PER_FILL=14` biomass-units ≈ a short hunting streak). ANY possession requires it FULL and `spend()`s it entirely on complete — so you must feed between takeovers (free-below-dominance grabs included). New purple `#resonance-bar`/`#resonance-fill` in health-hud (glows when `.full`, pulses red `.needed` when you're aiming at a takeable fish but under-charged). `onFull` → purple "RESONANCE FULL · possession ready" toast (`#stage-toast.resonance`).
  2. **Possession only on a TARGET** — `PlayerPossession.update(dt, target)` now acts ONLY on the passed locked-on creature (from `GameApp.lockedCreature` / hold-RMB lock-on). No lock target → `bestTarget=null` → possess prompt HIDDEN. Dropped the old forward-cone `scan()` (renamed `markEligible()` = sets `stunReady` purple bars on ALL takeable creatures; `candidate()` = the locked target if alive+in-range+eligible; `eligible()` = free-below-dominance OR weakened-past-`requiredFrac`). Prompt shows only when: locked target + eligible + in range + resonance full. `needsCharge` flag drives the meter pulse. `updateLockOn()` moved BEFORE `possession.update` in tick.
  VERIFIED (browse real GPU): fill math 0→0.5(feed7)→1.0(feed7); no-target→no prompt; target+empty→hidden+needsCharge; target+full→bestTarget=target; full channel→resonance spent to 0 + host swapped + growth adopted; tsc + build clean; no console errors. Design choices to flag at playtest: `RESONANCE_PER_FILL=14` (fill speed), and possession requires holding RMB (lock) + F together.
- **Log 24 (2 QoL additions on the Log-23 build, awaiting approval)** — resumed after prior session hit its usage limit mid-edit (only `Input.rmbDown` field had been added).
- **Log 24 (2 QoL additions on the Log-23 build, awaiting approval)** — resumed after prior session hit its usage limit mid-edit (only `Input.rmbDown` field had been added).
  1. **Free possession below Dominance**: `Dominance.canFreelyPossess(species)` — a creature whose class sits below your rank is possessable at ANY health (no weakening needed). `CLASS_PEER_RANK` {weak:0, medium:1, strong:2, apex:3}, free if `rankIndex > peer`. So weak prey become free grabs at Hunter, foragers at Predator, predators at Abyssal, apex at Usurper. `PlayerPossession.scan()` now: `eligible = canFreelyPossess || (weakened past requiredFrac)`. Verified: Fry not-free@Drifter → free@Hunter; Grouper not-free@Hunter → free@Abyssal.
  2. **Hold-RMB lock-on**: `Input` wires RMB (mousedown/up button 2, contextmenu prevented, cleared on blur/unlock). `PlayerCamera.setLockTarget(pos|null)`/`isLocked` — while locked, manual mouse-orbit is suspended and the orbit swings so the target sits centered ahead of the host (`wantYaw=atan2(-dx,-dz)`, pitch from -dy/len), so W-swim + bite + possess all line up. `GameApp.updateLockOn()` (sticky target: keep while alive & <78 m, else `acquireLockTarget()` = nearest in-cone within 60 m, cone dot 0.3), `updateLockReticle()` projects a cyan `#lock-reticle` ring onto the target. Controls hint + reticle DOM/CSS added. Verified: camera aim dot 0.112→0.997 after locking a side target; reticle + rmb field present; cleared on death.
  VERIFIED (browse real GPU): both features' logic + camera rotation live; tsc clean; no new console errors (only pre-existing flora `side` warnings). Dev server left running on :5173 for user play-test.
- **Log 23 (Phase 7 critical fix pass, awaiting approval)** — 9 fixes on the Log-22 build.
  1. **Sizes down + count −26%**: `GROWTH_MAX_LENGTH` 6→5, `WILD_SIZE_SKEW` 2.6→3.2, and every species' `wildMaxGrowth` cut (prey 0.8-1.0→0.42-0.6, predators 0.5→0.32, shark 0.32→0.22). Pop 222→164 (verified maxes: silverside 3.8, grouper 6.1 vs the old 8-9 m; medians ~1.5-3 m).
  2. **Fewer schools, spread across the map**: only fry/clownfish/angelfish school now (silverside/anchovy/sardine → `schooling:false`, solitary). Ecosystem REWORKED — creatures spawn/respawn/roam uniformly across the whole shelf (`habitatSpot()` replaces the player-ring `ringSpot`); removed the school "home-pull". Verified: spread box ≈ full habitat, 51% of creatures hidden (>150 m, cheap).
  3. **30 s spawn-safe bubble**: `Ecosystem.armSpawnSafe(x,z,30)` (SPAWN_SAFE_R=46) — creatures (fish + crabs) are repelled from it and predators can't bite inside it; armed on beginPlay + each descent. Verified: 0 creatures within 46 m of spawn.
  4. **Perf** (done last): draw calls 272→**121 typical / 191 worst-case (map center)**; now below the pre-Phase-7 194-fish/90 fps baseline. Fewer + smaller + spread (most hidden) fixed the 90→60 drop. Far-offscreen move throttle 3→8 frames.
  5. **No agility/turn penalty** when growing: `PlayerGrowth.agility` → always 1.
  6. **Hold-to-possess**: PlayerPossession reworked from an instant F-press to a 3 s HOLD channel (`input.isDown('KeyF')`) — host freezes, target stunned, channel bar fills, release/leave-range cancels, complete → swap. Range = 12 + len×4. Verified: begin→50%@1.5s→complete, cancel-on-release, camera transfers.
  7. **Camera** sqrt-dampened: `computeBaseDist = distanceFactor × sqrt(length)` (DARTFISH factor 4.2→3.0, host 4.3→3.1). Big fish fill more screen. Verified: 1.55 m host → camDist 3.7 m (was ~6.7 m linear).
  8. **Predators bite mouth-only**: `strikePlayer` uses the head's actual heading (from yaw/pitch), requires the player in a tight front cone (dot>0.6) AND within a snout `gape` — no more side/body damage.
  9. **Player bite mouth-sized**: reach `len×1.7+0.4`→`len×0.9+0.25`, cone dot 0.5→0.6, origin at the snout. Only what the lunge sweeps in front is bitten.
  VERIFIED (browse real GPU): all of the above; tsc + build clean; no console errors. Removed dead `Input.consumePossess`.
- **Log 22 (Phase 7 + size overhaul, awaiting approval)** — Built Phase 7 plus the user's 4 asks.
  1. **Universal size/growth** (`src/data/growth.ts`): every fish spans min length → `GROWTH_MAX_LENGTH=6`× (216× volume; user wanted "way bigger" than the old 4.4×). `growth01` 0..1 places size; `healthAt`(HP_SIZE_EXP 1.4) + `biteScaleAt`(BITE_SIZE_EXP 1.3) tie HP/bite to size; `PLAYER_HP_MULT=5` (player host tankier than wild). `rollWildGrowth(wildMaxGrowth, skew=2.6)` = heavily small-biased. Wild fish spawn at varied sizes per type (schools share a base size + jitter via `SchoolRef.growth`). GrowthDef refactored to {baseHealth, biomassToCeiling, stages}; DARTFISH baseHealth 100 / biomass 70. Creature `growth01` field drives length/HP/scale on spawn (replaced sizeVar). Verified sizes: fry 0.4-0.69, clownfish 0.5-1.11, angelfish 1.0-2.77, anchovy 1.5-6.5; big predators capped (shark wildMaxGrowth 0.32, crab 0.35).
  2. **Possession** (`src/player/PlayerPossession.ts`, `F` key via Input.consumePossess): weaken a creature below a size-scaled threshold (`requiredFrac = clamp(0.42/ratio + rank*0.04, 0.06, 0.55)` → BIGGER=harder) → `Creature.stun()` freezes+wobbles it → F launches a dash-in takeover → `PlayerFish.swapHost(profile, factory instance)` wears its body at its natural size, `growth.setHost(target.growth01)` (possessing a max fish → growth bar MAX), HP = max(target.health, 55% of new max), old body `die()`s (no dup, respawns later). `hostProfileFromCreature()` derives a SpeciesDef from any creature (movement straight from it, camera/growth from size). Camera transfers seamlessly (same `fish.object`). Dominance persists. HUD: purple "◇ F POSSESS <name>" prompt + purple pulsing stun-ready HP bars + "Possessed ·" toast + `#possess-flash`. Synth sfx `stunHit()`/`possess()`.
  3. **Accurate bite** (point 1): reach = `length*1.7+0.4` (0.5 m host → ~1.25 m, was a flat 5.5 m). Verified: 1.2 m fish bitten, 3.0 m NOT. Bite damage now length-based `34*(len/0.5)^1.3` (removed growth biteScale).
  4. **New fish**: `clownfish` (same model as player; 24 in 3 schools) + `angelfish` regal_angelfish.glb (20 in 2 schools). Both render + orient correctly on the real GPU (angelfish yellow/blue, head-first). Trimmed silverside 48→40, anchovy 44→36 to offset draw calls.
  VERIFIED (browse real GPU): full possess chain (host swap + growth/HP transfer + size), size-difficulty (5.4× target needs <7.8% HP), max-growth possession → growth 0.97/"Prime", accurate bite radius, possess UI renders, no console errors, tsc+build clean. Perf: 222 creatures, **272 draw calls** (was ~243; the +44 fish), 3.56M tris, quality high. Connection deferred to Phase 9 per user. Known limit: resume-after-reload always restarts as the starter clownfish (hostSpeciesId saved but resume is depth-only); possessing a crab uses fish swim (edge case).
- **Stack**: Three.js 0.185 + TypeScript + Vite 8, no physics engine, HTML-overlay UI, custom kinematic swim + analytic-heightfield collision
- **Git**: repo initialized on `main`, remote = https://github.com/Arjun0014/404-Hz-Borrowed-Bodies
- **Hosting**: wavedash (decided by user; only matters at Phase 19/22)
- **Assets in use**: `assets/clown_fish_compressed.glb` = starter fish (Draco+KTX2, 276 tris, 94 bones, 1 anim clip); `assets/tuna_fish_compressed.glb` reserved for Phase 3+ (6k tris, 47 bones, 2 clips). Licence info still TBD from user (`assets/LICENSES.md`).
- **Run**: `npm run dev` → http://localhost:5173, click to dive. F3 stats, F4 quality.
- **Next action on approval**: Phase 3 — ambient ocean population (schools, tier system, spawn/despawn, population budgets, distance-based AI simplification)

## Architecture (Phase 2)

- `src/world/types.ts` — Zone interface + shared types (CylinderCollider, TerrainMaps, TerrainLike, ZoneBounds, DescentTrigger, DescentInfo). ALL zone/player code imports collider/terrain types from here now (moved out of ShallowVeil.ts and Terrain.ts).
- `src/world/ZoneManager.ts` — owns single active zone; createZone(depth) factory (0=ShallowVeil, 1+=BlockoutZone), disposeCurrent, promote, snapshot() (renderer.info + heap).
- `src/world/BlockoutZone.ts` — temporary lower-zone prototype (NOT Drowned Garden art): dark enclosed bowl, blockout pillars w/ colliders, glowing descent core, darker per depth. Own BlockoutTerrain (bowl heightfield).
- `src/state/RunState.ts` — persistent run state (hostSpeciesId, depth, score, dominance, stats, seed). In-memory survives transitions; localStorage autosave on descent → resume-after-refresh. RunState.clear() for new run.
- Player rig (fish, SwimController, PlayerCamera, Bubbles, UnderwaterFx) is PERSISTENT across zones. Controller/camera have bindZone() to swap terrain/colliders/bounds/spawn on descent. Controller uses per-zone ZoneBounds (ceiling/radius/center) not global WORLD consts; pit-upwelling removed.
- Descent flow in GameApp: enter trigger volume → HTML prompt (#descend-prompt, E=descend/Q=cancel) → doDescend(): dark transition overlay → dispose old → build new → rebind player → RunState.descend()+save → reveal. Zone tag HUD (#zone-tag). __game.runTransitionTest(n) for memory test.
- KEY: zone.dispose() must NOT null scene.fog/background (global render state; next zone sets them). Terrain.dispose() must NOT dispose maps (zone owns clones). Each zone clones base seabed textures for terrain+rocks and disposes the clones.

## Key files

- `IMPLEMENTATION_PLAN.md` — full Phase 0 plan (architecture, perf budget, all 22 phases, risks, asset plan)
- `GAME_DESIGN.md` / `IMPLEMENTATION_PHASES.md` / `ASSETS.md` — design authority (do not edit without user)
- `PLACEHOLDERS.md`, `ASSET_MANIFEST.md`, `assets/LICENSES.md` — created in Phase 1

## Standing rules (read every session)

- One phase per implementation pass; stop and wait for explicit user approval at each gate.
- Record a performance measurement (fps, frame ms, draw calls, heap, renderer.info) at the end of every phase, here.
- 5× zone-transition memory test whenever zone code changes.
- Every placeholder gets a row in PLACEHOLDERS.md; every external asset gets a row in assets/LICENSES.md.
- Update this file at the end of every iteration.

## Log

- **2026-07-19 (20)** — Phase 6 built: DOMINANCE (Phase 5 committed+pushed c29b41e first). Persistent run-level rank earned by DEFEATING creatures (not eating). NEW `src/systems/Dominance.ts`: RANKS Drifter(0)/Hunter(25)/Predator(90)/Abyssal(260)/Usurper(650); `classify(species)`→ weak(prey,val1)/medium(forager+crab,4)/strong(predator,14)/apex(shark,35); per-class CAP weak30/medium95/strong280/apex∞ (the anti-farm core: a class can't push Dominance past its cap → past the cap kills give ×0.05). Per-kill gain = value × firstKillBonus(2.5 first defeat of a species/run) × falloff(1/(1+kills×0.5) same-species diminishing). Backed by RunState.data.dominance + new `speciesKills:Record<>` (persists across hosts+descents; save on rank-up + descent). Ecosystem `onPlayerKill(c)` fired in playerBiteCone on kill → dominance.recordKill(c.species). GameApp: dominance wired, HUD #dominance-hud (top-left under zone-tag: rank + blue progress bar), rank-up reuses #stage-toast (.dom blue) + hud flash, onWeakCapped → existing showHint("hunt bigger creatures"). VERIFIED (browse): first fry 2.5 (bonus) then 0.67/0.5 (falloff); **200 fry farm = only 11 pts = still Drifter** (can't reach Hunter let alone Predator ✓ anti-farm); weak VARIETY (fry+silverside+anchovy+sardine ×60) caps at 30.6 = Hunter + weakCapHint fired; medium→Predator(98); groupers→Hunter, +shark→Predator(171). HUD shows rank+bar; **Dominance persists across descent (55→55, depth 0→1)**; 5× transition memory test FLAT (heap 65, tex 227→40); tsc+build clean, no errors. Data ready for possession gating (Phase 7). NOT committed (approval gate). Note: exact values/caps are tuning hooks, not final balance.

- **2026-07-19 (19b)** — Phase 5 growth economy retune per user ("scaling too fast, need way more; bite = a size-based chunk; whole fish worth way more; big-fish chunks worth more but riskier"). Ecosystem.playerBiteCone biomass rewritten: every bite that LANDS (chip OR kill) yields `length × BITE_CHUNK(0.4)` (a chunk, bigger for bigger fish), plus `length × FINISH_BONUS(1.0)` when it dies (finishing/eating whole). Removed PlayerGrowth GROWTH_RATE (feed() now adds biomass directly). DARTFISH biomassToCeiling 55→60. Result: fry ≈0.4 biomass (~150 fry to max, ~3.6× slower than the old 41), silverside ≈1.9 (kill, ~32 to max), grouper/shark chunks worth much more over multiple bites (risky). VERIFIED (browse): fry bite 0.40/fry, silverside 1.88/fish, 1 fry=0.7% / 10 fry=6.7% growth, no errors, build clean. Early game you can also DAMAGE (chunk) bigger fish you can't yet eat whole — risk/reward path. Still awaiting Phase 5 approval; NOT committed.

- **2026-07-19 (19)** — Phase 5 built: SPECIES GROWTH (Phase 4 committed+pushed f06cc66 first). Eating biomass grows the starter host toward its species ceiling. NEW `src/player/PlayerGrowth.ts`: biomass tracking (GROWTH_RATE 4.5 × consumed body-length), progress 0..1, continuous size lerp base→ceiling, named stages, ceiling clamp. `SpeciesDef.growth` (GrowthDef: ceilingLength, biomassToCeiling, maxHealthBase/Ceiling, biteScaleCeiling, stages[]); DARTFISH growth = ceiling 2.2 m (4.4×), biomass 55, HP 100→240, biteScale→2.4, stages Fry/Juvenile(.25)/Adult(.55)/Alpha(.85); TUNA got one too (interface now requires it). PlayerFish: `baseLength` const + mutable `length` + `agility` + `setGrowth(scale)` (scales `object`, keeps tail math in base units); growth also pulls camera back via camera.setHost(profile,length). PlayerCombat: `biteScale`, `onFeed(biomass)` cb; resolveBite reach = max(5.5, 3+len×2.4) and damage ×biteScale and eatMax=len/1.25 all GROW → bigger host eats bigger prey (at 2.2 m eatMax 1.76 → can eat silverside/anchovy). Ecosystem.playerBiteCone now also returns `biomass` (sum of eaten/killed body lengths). SwimController turn uses `fish.agility` (1→0.65 at ceiling = heavier=less nimble drawback). GameApp: growth wired (combat.onFeed→growth.feed, onStageUp→toast), growth HUD (#growth-bar amber→teal-when-MAX, #growth-stage, #growth-len) + #stage-toast. VERIFIED (browse): feed 0→0.5→1.0 progress → len 0.5→1.36→2.2, scale ×4.4, maxH 100→171→240, biteScale 1→1.71→2.4, agility→0.82, atCeiling+MAX at ceiling; camera baseDist 2.1→9.5 m (settled); eatMax 1.76 at max; end-to-end combat.onFeed(2.4)→len 0.83 grows; HUD "ALPHA · MAX / 2.2 m" full bar; grown fish visibly huge (screenshot). 5× transition memory test FLAT (heap 65, tex 227→40). tsc+build clean, no errors. NOT committed (approval gate). Growth persists across descent (same host).

- **2026-07-19 (18)** — Phase 4 audio/lunge tuning pass (still awaiting approval). User added `assets/sounds/shadow_veil_ambient.wav` (21 MB) + `drowned_garden_ambient.mp3` (reserved for deeper zones). (1) fish_swim LOUDER: setSwim gain ×0.08/0.16→**×0.22 normal /×0.36 sprint, cap 0.16→0.45** (was near-silent). (2) LUNGE distance ×0.65: LUNGE_SPEED 30→20 (verified 10.2 m→**6.8 m**); ATTACK_CD 0.55→**2.0 s** (no click-spam, one committed lunge). (3) NO player bite-sound STACKING through schools: `landedThisLunge` flag → biteLanded() plays at most once per lunge (biteSwing already once/lunge; predator bites unaffected). (4) ZONE AMBIENT: `Sfx.playAmbient(url,gain=0.16)` loads+loops a bg track (caches by url, swaps on zone change, 1.2 s fade-in, guards zone-change-mid-load); `AMBIENT={shallowVeil:shadow_veil_ambient.wav, drownedGarden:drowned_garden_ambient.mp3}`. GameApp.updateZoneAmbient() (depth0→shallowVeil else drownedGarden) called on beginPlay + descent. VERIFIED (browse): ambient fetch+decode OK, looping src playing at gain 0.16, cached; lunge 6.8 m; no errors; tsc+build clean. Audio timbre/levels still user-judged (headless can't render output dynamics). NOT committed (approval gate).

- **2026-07-19 (17)** — Phase 4 audio→real samples + BIGGER LUNGE (still awaiting approval). User added `assets/sounds/`: fish_swim.mp3, bite_landed.mp3, random_bite_sounds.wav (6.4 MB). (1) Sfx.ts rewritten to load REAL samples (fetch+decodeAudioData on first gesture, each loads independently so small ones are ready fast): REMOVED synth ambient bed + synth swim whoosh. `setSwim(speed01,dashing)` = looped fish_swim, LOW gain (≤0.16, ×0.08 normal/×0.16 sprint) + playbackRate 0.9→1.9, tracks real speed (0 when idle = silent). `biteSwing(gain)` = random SLICE of random_bite_sounds.wav (0.28-0.5 s at random offset, faded edges → varied per bite). `biteLanded()` = bite_landed.mp3 (meat chunk). death() still synth. Call sites: player lunge→biteSwing; player bite connects→biteLanded+camera.punch(9); player hit by predator→biteSwing(0.8)+biteLanded. GameApp: startAmbience→load(), updateAmbience→setSwim. (2) BIGGER LUNGE — **ROOT CAUSE of "gimmicky": SwimController clamped vel to maxSpeed EVERY frame, killing the burst instantly.** FIX: `lunge()` sets `lungeBoostT=0.45` → during boost, drag ×0.35 and the max-speed cap is LIFTED (cap=max(maxSpeed,speed)) so the burst glides then eases back. Plus LUNGE_SPEED 16→30, BITE_REACH ~3→5.5, ATTACK_WINDOW 0.32→0.42, camera.punch 10→18, squash-stretch z 0.4→0.55. VERIFIED (browse): samples fetch 200 + decode OK (swim/landed/bites buffers non-null), ctx running, all sound methods no-throw; **lunge now carries the fish 10.2 m over 0.4 s (was ~2.5 m clamped)**, speed 30→23 gliding; no errors; tsc+build clean; perf unchanged (242 calls, 3.55M tris, 194 creatures). Can't verify audio *timbre* or swim-gain dynamics headlessly (AudioContext runs but automations need a real gesture/output) — user must judge sound. NOT committed (approval gate).

- **2026-07-19 (16)** — Phase 4 COMBAT FEEL + AUDIO OVERHAUL per user feedback (still awaiting approval gate). (1) NO SPAWN-TIME ATTACKS: predators were hunting/biting the stationary host on the loading screen (combat.takeDamage ran before `started`). Fix: `combatActive = started && !dead && !transitioning && spawnGrace<=0` passed to ecosystem.update as the huntable flag → no hunting/biting before control or during a **3.5 s post-spawn grace** (3.0 s on descent). Plus ecosystem `SPAWN_CLEAR=34` — no creature spawns within 34 m of the player at zone load (they home-pull in after). (2) LUNGE ATTACKS, FRONT-ONLY (no more side-touch damage): player left-click = committed forward LUNGE (SwimController.lunge, speed 16 — real distance, doubles as escape) + a live bite window (0.32 s); `ecosystem.playerBiteCone(origin,forward,reach,minDot=0.45(~63°),eatMax,dmg,alreadyHitSet)` only bites creatures in the FRONT cone, dedup per lunge so it plows through schools. Predators rebuilt too: `Creature.strikePlayer` — lunge (vel burst 1.7×maxSpeed) when in range + facing, damage lands ONLY during the 0.45 s lunge window, front-on (dot>0.25). PLAYER_BITE_DAMAGE grouper10/barracuda15/shark34. (3) ATTACK/KILL ANYTHING + HP BARS: bite damages any non-fry (schools, foragers, predators, crabs) via takeDamage; fry (≤eatMax) still instakill+eat(+14 heal); non-fry kills give +6. `Creature.hpBarTimer=4` on damage (decremented in move); new `src/ui/DamageBars.ts` projects floating HP bars (DOM pool of 14) over recently-hurt on-screen creatures, nearest first. (4)+(5) AUDIO fully rewritten `src/core/Sfx.ts` → procedural engine (layered noise+filter+envelope, NOT beeps): lunge(whoosh), chomp(click+snap+thud), eat(gulp), kill(crunch), hurt, death; + CONTINUOUS beds: underwater ambience (looped noise→lowpass 320Hz + LFO) and speed-driven SWIM WHOOSH (bandpass gain/freq track speed, brighter when dashing) via startAmbience()/updateAmbience(dt,speed01,dashing). Ambient bubble blips. NOTE: synthesized (no sample files) — call sites stable for a Phase-20 sample swap. (6) BIGGER CRAB: baseLength 1.35→2.7 (now ~2.3-3.0 m, clearly visible). JUICE: lunge → camera FOV punch (PlayerCamera.punch/kickFov), PlayerFish squash-stretch (lungePulse), bubble burst (auto via speed spike). VERIFIED (browse): no load-screen attack (health 100, nearest predator 29 m idle before start); grace holds; player lunge damages bigger prey (silverside→1 HP, only 1 in cone not all); 8 HP bars render+position correctly; predator lunge connects front-on (100→90 then satiates+leaves); audio methods all run no-throw; crab 3.0 m visible; front cone dedup works. Perf: draw calls 240, tris 3.55M, heap 111 MB (FLAT across 5× transition test, tex 226→40). tsc+build clean, no errors. NOT committed (approval gate). Can't verify subjective sound/feel headlessly — user must judge.

- **2026-07-19 (15)** — Phase 4 built: HOST HEALTH, DAMAGE, FEEDING, DEATH (survival loop) — **awaiting approval gate**. Design gap: 0.5 m starter clownfish is smaller than every creature → nothing to eat; added tiny **fry** species (fishSchool1 model @0.3 m, schooling, role prey, POP 44 in 2 bait balls) as its food. NEW: `src/player/PlayerCombat.ts` (host health 100, no passive regen except 3/s after 5 s out of combat; **left-click bite** = lunge + jaw hitbox 1 m ahead; eating fry heals +14 each; predator damage w/ 0.5 s i-frames; death). `src/core/Sfx.ts` (WebAudio placeholder blips: bite/eat/thud/hurt/death; lazy AudioContext on first gesture). Input.consumeAttack() (left-click edge, only when pointer-locked). SwimController.lunge()/getForward(). Creature: `health`/`maxHealth`(=8+len*16)/`takeDamage()`; predators now HUNT THE PLAYER (canEatPlayer + huntPlayer flag; tryBitePlayer w/ PLAYER_BITE_DAMAGE grouper10/barracuda15/shark34, 1.1 s cd); prey/foragers FLEE when player attacks (ctx.playerThreatT set by bite). Ecosystem: onHitPlayer callback, playerThreatT, `playerBite(point,radius,eatMaxLen,damage)` (uses per-frame hash; instantly eats edible ≤eatMax, chips bigger; returns {hit,eaten}); update() takes playerAlive. GameApp: combat wired (onHitPlayer→takeDamage, onDeath→death screen, onHit→screen-shake), tick gates control on !dead, updateCombatHud (health bar + red vignette from hurtFlash), R restarts run. HUD (index.html+style.css): #health-hud bar (low=red/fed=green), #damage-vignette flash, #app.hit shake, #death-screen ("YOUR HOST HAS DIED / press R"). **KEY AI FIXES for predator-vs-player**: (1) predators were never closest in the dense crowd → hunt player when <14 m OR <preyDist+6; (2) predators ORBITED (turn radius > bite reach) → slow to boost 0.6 when <6 m + bite reach = radius+len*0.5+2.0; (3) predators held off at CROWD EDGE by separation + a wrongly-applied player-scatter → excluded predators from player-scatter, and a hunting predator uses light separation (0.4) to barrel through. VERIFIED (browse): eating (bite fry ball → 4 hit/4 eaten, heal +14/fry), damage (100→70, vignette 0.675, shake), death screen + unlock, predator full loop (grouper closed 9→3 m through crowd, bit 100→80, satiated hunger→left). Perf: **draw calls 243 (near 250 ceiling), tris 3.55M @ FLORA_DENSITY 2.0, heap 65 MB, 194 creatures**. 5× transition memory test FLAT (heap 65, tex 227→40). tsc+build clean, no errors. NOT committed (approval gate). Watch: draw calls near 250 (fry +44 creatures); combat is close-range so predators must commit — tuned but user should feel it.

- **2026-07-18 (14c)** — Corals BIGGER (user: "corals 3× standard, up to 10× max"). Bumped coral ranges in Flora.ts KINDS: coral2 2.4-8.0 (was 0.8-4.2), spiked 1.5-5.0 (was 0.5-2.6), coral1 10-30 (was 4-20), coralPiece 8-26 (was 3-15). Added a WATER-COLUMN CLAMP in scatter(): every instance's height is capped to `WORLD.surfaceY - gy - 2` so nothing pokes through the y=45 surface (matters for giant corals on high mesa tops; also protects tall plants). VERIFIED: tallest flora instance top = y42.6 (<45, no breach); coral heights now coral2 2.4-8, spiked 1.5-5, coral1 12-21, coralPiece 11-26 (giants auto-clamped by column); big red coral_1 + purple coral_piece render as prominent landmarks (screenshot). Scaling adds no tris — still 3.5M @ FLORA_DENSITY 2.0, ~189-224 draw calls, no errors, build clean. Not committed.

- **2026-07-18 (14b)** — Flora SIZE VARIATION (user: "all assets varyingly bigger, same size up to 5×, varyingly heighted"). Widened each KIND's minH→maxH to a ~5× spread in Flora.ts: lowplant 0.7-3.6, shrub 1.0-5.2, marineLow 2.2-11, marine 2.2-12, lumin 2.6-13, coral2 0.8-4.2, spiked 0.5-2.6, coral1 4-20, coralPiece 3-15 (giant corals capped <20 m so they stay below the y=45 surface). Scaling is uniform → adds ZERO triangles (only height/width vary). VERIFIED: instance scale ranges confirm ~5× spread per kind; screenshots show tiny ground ferns → tall broad-leaf plants → giant glowing luminescent towers in one frame — dramatic variation. NOTE: user had bumped **FLORA_DENSITY 1.0→2.0** on disk (the dial) → 5705 instances, **tris ~3.5M**, still ~217-224 draw calls (instancing). Left FLORA_DENSITY at their 2.0. tsc + build clean, no errors. 3.5M tris is heavy — if fps drops, lower FLORA_DENSITY. Not committed.

- **2026-07-18 (14)** — SEABED FLORA FOREST (user: "make the biome look alive, add a lot of plants, forest down there"). New `src/world/Flora.ts` — GPU-instanced flora system mirroring the Ecosystem shape (load 9 GLBs once → normalized templates; `bindZone(terrain, area, colliders)` scatters InstancedMeshes on the shelf; `update(dt)` sways; `unbind`/`dispose`). Each kind = 1 InstancedMesh = ~1 draw call regardless of count (coral_piece = 2 mats = 2). `prepare()` merges each model's meshes per-material, normalizes to base y=0 + centred, measures nativeH/nativeR. Placement = slope-limited shelf scatter (uses zone getPopulationArea rect) mixing cluster-seed patches + open scatter; per-instance scale to a target height + random yaw. KINDS (count@tris, FLORA_DENSITY dial default 1.0): lowplant 2000@192 (the carpet), shrub 300@800, marineLow 50@3040, marine 100@1464, lumin 7@26367 (EMISSIVE cyan glow 0x2effe0 — bioluminescent accents, kept sparse: 26k tris each), coral2 150@1168, spiked 240@646, coral1 5@7372 (huge, COLLIDER), coralPiece 6@7375 (big, COLLIDER). Big corals push cylinder colliders into zone.colliders (→ 51 total; fish+player avoid them). **KEY BUG FIXED**: plant GLBs use ALPHA-CUTOUT leaf textures — forcing them opaque (like fish bodies) drew the transparent quad backgrounds as solid BLACK. Fix in makeMaterial: detect alpha (src.transparent || alphaTest || alphaMap) and for foliage set `alphaTest=0.5` + `transparent=false` + DoubleSide (cutout, not blend) → clean leaves, correct sort. Sway = onBeforeCompile injecting `position.y/nativeH` height-weighted sinusoid (same pattern as ShallowVeil.makeSwayMaterial but keyed on local Y not uv). GameApp wiring: Flora field; load + bindFlora(zone) BEFORE ecosystem (so coral colliders are in the shared zone.colliders array the controller/camera/ecosystem all reference); flora.update(dt) each tick; bindFlora(next) on descent before rebinds; blockout (area null) → flora.unbind(). __game.flora exposed (it's a GameApp field). VERIFIED (browse, real-GPU): 2858 flora instances in only **~10 draw calls** (total 180-224 calls); **NO shader errors** (sway + emissive compile clean); big red coral_1 + glowing luminescent clusters + green marine plants + fern/shrub carpet + seagrass all render correctly (screenshots). Perf: **tris jumped 590k → ~2.0-2.1M** (static instanced, low draw-call cost; the carpet + luminescent dominate), heap 54→87MB. **5× transition memory test FLAT** (heap 87 across all 5; flora freed on descent 2858→0, tex 206→39; templates persist by design). tsc + vite build clean. NOT committed (awaiting approval). PERF LEVER: lower `FLORA_DENSITY` in Flora.ts (scales all non-collider counts) if user's fps drops from the ~2M tris; luminescent (26k×7) and lowplant (192×2000) are the biggest tri chunks. Plants render a bit dark (moody zone lighting) — could add small emissive/lower roughness if user wants them brighter.

- **2026-07-18 (13)** — Phase 3 ECOSYSTEM REWORK per user feedback (4 issues: few fish visible / fish over cliffs; no wall+terrain+rock collision; shark weird+passive; dead ecosystem/no food-fighting/no replenish). ROOT CAUSES: (a) fish spread over the whole ~250×280 m shelf → <20 visible through fog; steer/clamp used the FULL zone box (maxX 250, over the deep) not the shelf → fish wandered past the cliff. (b) collision was terrain-floor (vertical) + clamp-to-full-box only → climbed walls/mesas, no cliff edge. (c) AI ran on staggered frames with `dt*stride` and only MOVED on the think frame → visible position stutter (worst on the big slow shark); apex fled other apex. (d) 10 predators over a huge shelf rarely met schools; respawn was an invisible teleport. FIXES: **Ecosystem = roaming "living bubble"** — a soft home-pull (Creature + school centre) keeps the whole ~174 population gathered within ~100 m of the player (pop-free, no teleports), so you're always in the middle of it. **Coherent shoals**: schools now have a roaming centre (Ecosystem.School / SchoolRef) members cohere to as one body; they recycle/rejoin together (school spread ~15-20 m). **think()/move() split** in Creature: decisions throttled by distance, physics INTEGRATE EVERY FRAME → smooth motion (fixed shark stutter); animation mixer throttled by distance (near=1, mid=2, far=3). **Shelf habitat**: ShallowVeil.getPopulationArea tightened to clear back/side walls (minX+55, ±(maxZ-57)) and stop short of the cliff (edgeX-12); Creature clamps + soft-steers to this habitat → 0 fish over cliff. **Collision**: terrain floor + look-ahead terrain avoidance (veer around mesas/pinnacles/walls) + rock-collider push-out AND steering avoidance → belowTerrain=0. **Predation chain**: shark(apex, no-flee, prowls)>predator>forager(eats small prey when hungry)>prey; crab ambush; canEat size-gated. Respawns swim in from a fog RING around the player (RING 55-115) or rejoin their shoal → visible replenishment ("from the deep"). **Perf-balanced counts** (leaned on cheap tris): silverside 64@534t, anchovy 52@806t, sardine 12@6.5k (was 24 — the tri hog), wrasse12/angel10/grouper7/barracuda5/shark2/crab10 = 174. VERIFIED (browse, real-GPU): 120 within 80 m (was ~12), 0 over cliff, 0 below terrain; shark hunts smoothly (bursts to 11.7=max×1.3 chasing prey); lively (84 school/50 flee/6-13 hunt/10 crab); pop self-stabilises at ~165 alive (8-10 cycling death→respawn). Perf: **204-214 draw calls, 571-590k tris, heap 54 MB — under 250/600k budget**. **5× transition memory test FLAT** (tex 195→27 creature bone-tex freed on descent, heap 54 flat). tsc + vite build clean, no errors. NOT committed (awaiting approval). Files: creatures.ts (tuning/counts/thresholds), Creature.ts (rewrite: think/move split, SchoolRef, habitat+terrain+collider collision, apex/forager rules), Ecosystem.ts (rewrite: schools, bubble/home-pull, fog-ring respawn, per-frame move + throttled think/anim), ShallowVeil.getPopulationArea (shelf habitat), GameApp.bindEcosystem (focus=player spawn). Perf lever if fps drops: trim silverside/anchovy counts (cheap) or lower FAR render distance in Ecosystem.

- **2026-07-18 (12)** — Phase 3 revision per user feedback (bigger/livelier/fixed). (1) SIZES: all fish now 2-7.6× the 0.5 m clownfish via bigger baseLength + per-instance `sizeVar` jitter (silverside 1.3, anchovy 1.5, sardine 1.9, wrasse 1.8, angel 2.1, grouper 2.9, barracuda 3.2, crab 1.35; shark 6.0). Creature.spawn takes lengthMul → sets inst.root.scale + length/radius (canEat size-gating now meaningful). (2) DENSITY DOUBLED + SHARK: SHALLOW_VEIL_POP=156 (was 78); added apex `shark` (shark.glb, apex:true, hunts everything incl. predators, senseRadius 40). Predators more aggressive: HUNT_THRESHOLD 0.5→0.3, higher hungerRate/senseRadius. (3) FIXED "SWIMMING SIDEWAYS": added per-species `modelYaw` override to CreatureFactory (replaces longest-axis heuristic). Calibrated via top-down renders (camera up=+Z, all set facing +Z): anchovy/angel/grouper/barracuda/shark already correct (native +Z); **silverside modelYaw=0.87 (~50° authored offset), sardine=0, wrasse=0**; crab left (claws +X = natural sideways scuttle). Added Ecosystem.paused debug flag (used for calibration). VERIFIED: 156 creatures, no errors; livelier — 55 school / 29 FLEE / 4 hunt / 7 crab (was 1-2 flee); shark renders huge (5.6 m) dwarfing player; corrected fish point head-first. Perf: draw calls 176, tris 535k, heap 69MB — under 250/600k budget. **5× transition memory test FLAT** (156 creatures: tex 164→27 on descent, flat across all 5, heap 68.9). NOT committed (awaiting approval); assets (incl. shark.glb, shark_megalodon.glb unused) still untracked. Perf levers if fps drops on user GPU: lower counts (sardine 6.5k-tris ×20 is heaviest; crab 5k ×10), or raise FULL/MID tier distances.

- **2026-07-18 (11)** — Phase 3 built: LIVING ECOSYSTEM (expanded well beyond the plan's "a few fish"). New assets (all Draco+KTX2): fish_school_1/2/3, random_fish_1-4, crab, + shark/shark_megalodon (registered later, NOT spawned in shallow — 23-27k tris). Asset budgets logged; fish_school_2=6.5k tris (kept school small), random_fish_4=64 bones, crab=0 bones/NO anim/11 meshes. NEW SYSTEMS: `src/data/creatures.ts` (data-driven CreatureSpecies: role prey/forager/predator/crab, size, diet, movement, senses; SHALLOW_VEIL_POP=78: 26 silverside+22 anchovy+10 sardine schools, 5 wrasse+4 angel foragers, 3 grouper+2 barracuda predators, 6 crab). `src/systems/SpatialHash.ts` (uniform 2D grid, O(n) neighbour queries). `src/entities/CreatureFactory.ts` (loads each glb once; SkeletonUtils.clone shares geo+mat, clones skeleton; forces opaque; CRAB merged to 1 geometry/1 draw call; per-instance skeleton.dispose() on despawn frees bone textures). `src/entities/Creature.ts` (per-entity state machine: wander/school[boids]/flee[size-gated]/hunt+eat/crab_scuttle/crab_jump[ballistic ambush]; steering integrator, turn-limited orient, terrain+collider+bounds collision, player-scare, mixer speed-scaled). `src/systems/Ecosystem.ts` (fixed POOL per zone; 3 distance tiers FULL<50m/MID<100/CULL<175 with frame-STAGGERED updates via phase%6 + dt*stride so anim stays real-time; predation eats prey→die()→respawn far = self-replenishing; queryNeighbors reused buffer, zero per-frame alloc). Zone gained getPopulationArea() (ShallowVeil=shelf box; Blockout=null→no ecosystem). GameApp: ecosystem.load() during loading screen, bindEcosystem on start + descent, update in tick, count in F3. VERIFIED (browse, real-GPU): 78 creatures, zero errors; schooling(29)/wander/hunt(1-2)/flee(1-2)/crab_scuttle(6) all live; predator eat → death→respawn confirmed (alive 78→77→78); CRAB AMBUSH verified (staged: y 29.7→35.1 apex→land, vy 9.5→0→-10.5, prey caught). Perf: draw calls 58-96, tris 300-411k, heap 69MB, textures~94 — all under budget (250 calls/600k tris). **5× transition memory test FLAT**: Shallow(78 creatures)→blockout freed geo 35→16, tex 94→21 (bone textures + zone), then 16/16 tex 21/21 heap 68.9 across all 5 — zero leak. NOT committed (awaiting approval); assets untracked. Perf levers if user's fps drops: lower SHALLOW_VEIL_POP counts (esp sardine 6.5k-tris), raise FULL/MID thresholds, or reduce mixer rate.

- **2026-07-18 (10)** — Three user bugs + darker shelf, all verified on real-GPU path via browse. (1) FISH TRANSLUCENT vs sky: root cause = clownfish GLB material exported `transparent:true, depthWrite:false` (opacity 1) → alpha-composited/washed out against bright surface. FIX in PlayerFish.ts: on load, traverse meshes and force any `transparent` material to `transparent=false; depthWrite=true` → writes depth, always opaque (verified solid against surface). (2) BRIGHT TEAL BAND at horizon: was the `buildEdgeGlow` marker (a `0x6cf5df` plane spanning Z at the cliff lip x=edgeX) — isolated by hiding surface (band stayed) then edge glow (band gone). FIX: removed buildEdgeGlow entirely (fields edgeGlow/edgeLight, build call, method, update pulse, PointLight import) — the dark abyss already signals the threshold; descent still triggers across the whole edge via descentX. (3) PERF 120→90 fps (fill-rate, NOT the darkening which is uniform/ALU-only): `Quality` high was rendering at pixelRatioCap **2** (=4× fragment work on hi-DPI) AND `UnderwaterFx` ran **3** full-screen passes. FIX: Quality caps high 2→1.5 / med 1.5→1.25 / low 1; UnderwaterFx folded OutputPass into the refraction ShaderPass (3→2 passes) doing ACES tonemap+sRGB in-shader — MUST set `pass.material.toneMapped=false` or three injects its own `RRTAndODTFit`/ACES → "function already has a body" shader error (hit + fixed). Brightness verified unchanged (exact three ACES matrices + exposure 1.18). (4) DARKER SHELF (user asked again): shallowColor 0x2c6d84→0x1f5165; hemi 1.15→0.82 base (update `0.82 - darkT*0.55`); sun 1.9→1.5. Build + tsc clean, zero shader errors, no band, fish opaque, deep still black. Draw calls 17-23, tris 268k, heap 43 MB. Still NOT committed (awaiting approval). NOTE: pre-existing harmless warnings remain — `THREE.Material: parameter 'side' has value of undefined` from coral `side: doubleSided?DoubleSide:undefined` (buildCoral); could set FrontSide later. Real-GPU fps must come from user (headless fps meaningless).

- **2026-07-18 (9)** — Finished the terraced-shelf descent refactor (from mid-edit last session) + darker/foggier water per user. GEOGRAPHY (was in progress, now complete & verified): the +X side is open sea; shelf ends at cliff lip `edgeX=66`, a terraced staircase (`stairProfile` in Terrain.ts) drops to a ~-177 basin; enclosing walls only on -X/±Z; descent prompt fires across the WHOLE edge once camera crosses `descentX=104` (not a ring). BUGFIX (the piece usage cut off on): `cancelDescent()` now sets `repelling=true`, so declining ("Q/stay") actively pushes the fish shoreward (firm -8 m/s current in `ShallowVeil.repelFromDescent`, verified pos.x 120→109→… back toward edgeX). DARKER WATER (new user ask): FOG darkened+denser (shallow `0x2c6d84`, deep `0x071a26`, new `abyssColor 0x02080e`, density 0.0058→0.0075); `ShallowVeil.update` now darkens by `max(depthSink, overDeep)` where `overDeep=smoothstep(edgeX-6,descentX+40,cam.x)` → fog lerps shallow→deep→abyss, density ×(1+darkT*1.7), hemi −0.72*darkT, god-rays fade. Terrain deep vertex colour near-black (`0.03,0.06,0.09`), deep lerp widened `smoothstep(6,-60,h)`, + shader abyss-darkening `diffuseColor*=mix(1,0.05,smoothstep(2,-42,worldY))` so the cliff face/basin geometry itself reads black. Deep silhouettes darkened. VERIFIED (browse, real-GPU): zero shader errors after terrain-shader edit; screenshots confirm lit shelf → dark drop-off → near-black abyss; prompt shows over deep; decline push-out works; 5× transition memory test flat (geo 8 / tex 7 / prog 8 / heap 61 MB). Also RESTORED `assets/tuna_fish_compressed.glb` (was deleted from working tree, still tracked in git, imported by species.ts → broke `vite build`; `npm run dev` was unaffected). tsc + `vite build` now both clean. NOT committed (awaiting user approval). Draw calls 21-27, tris ~250-268k.

- **2026-07-18 (8)** — Phase 2 built: zone lifecycle + one-way descent. Refactored to a Zone abstraction (types.ts) with ShallowVeil + BlockoutZone implementing it; ZoneManager owns the single active zone; RunState (persistent, localStorage autosave + resume). Player rig now persists across zones and rebinds (bindZone) on descent. Descent: enter trigger → "DESCEND TO X? There is no return" prompt (E/Q) → dark transition (fade + streaks) → dispose old + build new + rebind + save → reveal. VERIFIED headless: prompt shows only in trigger; Q cancels without descending (depth stays 0); E descends (depth 0→1, saved=1); **5× transition memory test FLAT (geometries 7/7, textures 7/7, programs 8/8, heap 54.2/54.2 MB — 0% growth)**; Shallow→blockout freed geometries 20→7 (heavy zone disposes); resume-after-refresh works (reloaded to depth 6); reset→fresh Shallow at depth 0; no console/shader errors. Blockout draw calls 9, tris 42k. Build clean. Awaiting user approval to proceed to Phase 3.

- **2026-07-18 (7)** — Watery feel + dash effect (35b695b). New `src/render/UnderwaterFx.ts` (EffectComposer: RenderPass → underwater ShaderPass → OutputPass): full-screen animated refraction wobble, light bluish depth vignette, speed-driven radial warp + chromatic streak (eases via smoothed uSpeed). New `src/entities/Bubbles.ts`: pooled 260-bubble system, ambient rising sprinkle around camera + dash burst from fish tail (streams backward). SwimController exposes `dashOutput` (full dashing, light cruising). PlayerFish.getTailPosition. GOTCHAS FIXED: (a) EffectComposer targets are linear → MUST add OutputPass or whole scene renders dark (missing tonemap+sRGB); (b) additive particles can't use scene fog (adds color) → fade alpha by distance in shader instead; (c) bubbles spawned near camera bloom into giant rings → clamp gl_PointSize + near-distance alpha fade; (d) composer multi-passes reset renderer.info → set autoReset=false + manual reset once/frame for correct debug draw-call count. Tuning: warp 0.005, chromatic 0.0005+spd*0.0022, vignette floor 0.9. Draw calls ~22, heap 38 MB. Awaiting user verdict.

- **2026-07-18 (6)** — Surface + sun looks pass (86aead9). User: surface looked weird (tiled sine bands), sun sprite's radial spokes looked bad (pinwheel). Fixes: (1) sun sprite texture now clean soft glow — bright tight core + wide soft halo, no streaks; scale 190, opacity 0.5. (2) surface shader replaced sine-product bands with domain-warped ridged noise caustic (2 octaves drifting opposite directions) → organic flowing dapples. Both verified via screenshots. Tick ~1 ms, draw calls 14-19, heap 48 MB. Phase 1 visuals now look good per repeated iteration; still need user's final Phase 1 sign-off to proceed to Phase 2.

- **2026-07-18 (5)** — God rays fixed properly (0584bff). Previous "camera-relative offset" fix was insufficient: it only tracked camera POSITION, not LOOK direction — standing still + moving mouse swept the fixed-orientation vertical planes across view and drove them edge-on (worst directly below the sun) → flicker. Replaced all 10 ray planes + separate glow sprite with ONE camera-facing Sprite (radial streaks + sun disc baked into a 512² canvas texture), anchored at camera.pos + sunDir*340 (infinitely-distant-sun behavior, no parallax). A Sprite always faces camera → cannot go edge-on or change brightness on orbit; sun translates smoothly across sky as you look around (correct), appearance invariant (fixed). Verified via 3 orbit-angle screenshots. Draw calls 40→~15. LESSON: never use flat billboarded planes for view-angle-sensitive sky effects near camera — use Sprites (auto camera-facing) or screen-space post.

- **2026-07-18 (4)** — Two user-reported bugs fixed (99c9f85): (1) "sunlight moves with mouse" = god-ray planes had fixed world orientations so orbiting changed their face-on brightness → rays now keep a constant camera-relative offset + facing yaw (brightness invariant under orbit; verified with two-yaw screenshot compare). (2) fish upside-down/slanted = `setFromUnitVectors` shortest-arc orientation accumulates roll → SwimController now tracks yaw/pitch scalars and rebuilds the quaternion via Euler YXZ (roll structurally zero; banking stays on modelRoot). LESSON: never orient bodies with setFromUnitVectors when an up-vector matters; never let view-angle-dependent billboards sit at fixed orientations near the camera. Draw calls 38-40 (rays unmerged, 10 draws), tris ~269k. Awaiting user GPU verdict on Phase 1.

- **2026-07-18 (3)** — Richer-world pass (user request): 14 mesas/pinnacles + 3 ridge walls baked into the heightfield (`FORMATIONS` in Terrain.ts — player+camera collision automatic); 34 textured rock spires clustered at formation skirts + 6 monoliths, all registered as cylinder colliders (`zone.colliders`, resolved in SwimController + PlayerCamera march; degenerate d=0 case handled — collider push-out verified via JS test); user's Poly Haven coral_fort_wall_02 1k texture (diff+normal, CC0) on terrain (repeat 110, vertex colors switch to tint-modulation mode when textured) and rocks/spires (cloned texture, separate repeat); coral doubled + 4th type (fan, CircleGeometry arcs) = 365 instances in 28 reef clusters hugging formations; 52 seagrass meadows; camera much closer (4.2×/1.6 m min) + snappier (pos k16/look k20, lookahead 0.07, FOV pump 7→4). Stats: 22 draw calls, 269k tris, 7 textures, 45 MB heap, tick ~1.2 ms. Build clean, zero shader errors on real-GPU path. Awaiting user GPU verdict.

- **2026-07-18 (2)** — Visual overhaul after user rejected first build ("no ground / looks crap"). ROOT CAUSE FOUND: shader variable named `patch` — a GLSL ES 3.0 reserved word — killed the terrain fragment shader on real GPUs → terrain never rendered for the user (lenient headless compile path hid it). Renamed, zero shader errors verified via browse console on the real-GPU path. Overhaul: fog density 0.0092→0.0058 + brighter blue palette; terrain rewrite (dunes + ridged rock lines, sand grain/ripple fragment detail, algae mats, richer vertex colors, 288 segs); dense dressing — 2500 swaying seagrass blades in 42 meadows, 3 coral types (branch/tube/mound via mergeGeometries, instanceColor), smooth boulders + monoliths, thicker kelp; merged-plane god rays following camera (fade with depth); brighter sun/hemi, exposure 1.18; camera closer (5.6×), fish 0.5 m, spawn 3 m above guaranteed reef+meadow. LESSON: instanceColor MULTIPLIES material color — use white base material. Draw calls 20, tris 235 k, heap 45 MB, tick 2.2 ms. Still awaiting user GPU verdict on Phase 1.

- **2026-07-18** — Phase 1 built: Vite+TS+Three scaffold; git init + GitHub remote; asset-report script (`npm run asset-report`); analytic-heightfield Shallow Veil terrain (rolling shelf, edge walls, drop-off pit at +X with soft pit floor until Phase 2); swim controller (scheme A camera-aim steering, drag, banking, dash stub; scheme B behind `STEERING_SCHEME` flag in `src/config.ts`); size-adaptive no-roll third-person camera with terrain collision pull-in; atmosphere (depth-graded FogExp2 + tone-mapped `scene.background` — NOT setClearColor, that mismatches ACES; shader caustics with depth+distance fade; wrap-around particle motes; surface plane + sun glow; instanced rocks/kelp; silhouette cones; pulsing descent marker + proximity hint); clownfish loaded via Draco+KTX2 with auto axis-align/scale + animation playback; debug overlay (F3), quality stub (F4). Headless-verified via browse: no console errors, swimming works, head leads motion, ~90 s zone crossing at cruise. Baseline (headless SwiftShader, GPU fps must come from user): 12-14 draw calls, ~145k tris, 38 MB heap, tick 0.9 ms avg. Known quirks: gstack browse daemon needed ACL repair on `.gstack` dir (icacls inheritance strip bug) — fixed; browse state dir = `<gitroot>/.gstack/`. Waiting: user approval of Phase 1 + fish licence info.
